import React from 'react';
import Button from 'components/Button/Button';
import './Root.scss';

const Root = () => (
  <div>
    <Button>Save</Button>
    <Button secondary>Remove</Button>
  </div>
);

export default Root;
